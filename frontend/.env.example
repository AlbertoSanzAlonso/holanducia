# Copiar a .env.local para desarrollo

# Vercel + API HTTP (IP:9000): NO definir VITE_API_URL (vercel.json hace proxy /api → VPS).
# Vercel + API HTTPS: VITE_API_URL=https://api.tu-dominio.com

# Desarrollo local: vacío (proxy vite.config.js → localhost:9000)
# VITE_API_URL=
